#include <butil/subint.h>

#include <catch.hpp>

using namespace butil;
using namespace butil::integer;

//using Pos = explicit_subint<interval_meet<int, uintmax_t>>;

TEST_CASE("Positive integer", "[subtype]")
{
//    CHECK_NOTHROW(Pos(0));
//    CHECK_NOTHROW(Pos(1));

//    CHECK_THROWS(Pos(-1));
}
